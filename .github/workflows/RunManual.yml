Name: Manual WorkFlow
On:
  workflow_dispatch:
  
  inputs:
  
  EmployeeName:
    description: this is Emp Name field
    type: string
    default: test
    required: true
  EmployeeAge:
    description: this is Emp Age field
    type: number
    required: true
jobs:
  build-manual:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "${{inputs.EmployeeName}}"

    
 
  
  
   
    
